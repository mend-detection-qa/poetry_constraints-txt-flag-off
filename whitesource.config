# PRD-8924 / SCA-5467 — constraints.txt support feature flag
# Gate proof: constraints.txt is present but the flag is OFF, so Mend must IGNORE it.
python.applyConstraints=false

# Poetry resolver settings (mirror existing poetry probes)
python.resolveDependencies=true
python.resolveSetupPyFiles=false
python.resolveHierarchyTree=true
python.resolvePoetryDeps=true